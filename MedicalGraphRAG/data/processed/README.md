# Processed data policy

Large generated artifacts are ignored by Git and must be reconstructed from the
versioned configuration plus source data. Only the small `audit_20.json`, its
directory README, and the final manifest are eligible for version control.

The canonical output directory is `pubmedqa_hard_v1/`.
